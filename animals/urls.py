from django.urls import path
# from .views import search_animal, register_animal

urlpatterns = [
    # path("search_animal/", search_animal, name="search_animal"),
    # path("register_animal/", register_animal, name="register_image"),
]
